var name = 'Omar'
var lastName = 'Cervantes'

var firstLetterNumber = name.charAt(0)
var lastLetterNumber = name.charAt(name.length - 1)
var fullName = `${name} ${lastName}`

var str = fullName.substr(2,4)
